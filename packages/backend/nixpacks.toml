# nixpacks.toml — Backend service Nixpacks configuration
#
# This file is used when the Railway backend service root is set to
# "packages/backend". It ensures the install step runs only once,
# avoiding EBUSY / ENOTEMPTY errors from nested npm ci calls.

[phases.install]
cmds = ["npm ci"]

[phases.build]
cmds = ["npm run build"]
