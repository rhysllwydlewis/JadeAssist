# railway.toml — Widget service reference configuration
#
# NOTE: This file is NO LONGER the active Railway configuration for the
# @jadeassist/widget service.  The widget service now uses a Dockerfile build
# and its Railway Root Directory must be set to "." (the repo root).
#
# When Root Directory is "." Railway reads the ROOT railway.toml (at the
# repo root), which contains the Dockerfile builder configuration.
#
# This file is kept for reference only.  See RAILWAY.md for full setup
# instructions and the correct Railway dashboard settings.
#
# Active configuration:
#   Root Directory  : .  (repo root)
#   Reads           : railway.toml  (repo root)
#   Dockerfile      : packages/widget/Dockerfile
