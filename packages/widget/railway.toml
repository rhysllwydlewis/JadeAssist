# railway.toml — Widget service (packages/widget directory)
#
# ╔══════════════════════════════════════════════════════════════════╗
# ║  ⚠️  THIS FILE IS NOT THE ACTIVE RAILWAY CONFIGURATION          ║
# ║                                                                  ║
# ║  The @jadeassist/widget service Root Directory MUST be set to   ║
# ║  "." (repo root) in the Railway dashboard, NOT "packages/widget"║
# ║                                                                  ║
# ║  If Root Directory is "packages/widget", the Docker build will  ║
# ║  FAIL — the build context won't include the root               ║
# ║  package-lock.json that npm ci requires.                        ║
# ║                                                                  ║
# ║  See RAILWAY.md for the correct Railway dashboard settings.     ║
# ╚══════════════════════════════════════════════════════════════════╝
#
# Active configuration when Root Directory is "." (repo root):
#   railway.toml (repo root)  →  builder = "dockerfile"
#                                dockerfilePath = "packages/widget/Dockerfile"
